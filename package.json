{
  "name": "quiz-game-monorepo",
  "private": true,
  "workspaces": [
    "server",
    "apps/host-react",
    "apps/player-vanilla"
  ],
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "install-deps": "pnpm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.5.1",
    "helmet": "^7.1.0",
    "socket.io": "^4.7.4",
    "uuid": "^13.0.0"
  }
}
