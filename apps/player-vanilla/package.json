{
  "name": "quiz-game-player-vanilla",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "python3 -m http.server 3000",
    "build": "cp -r . dist/",
    "test": "jest",
    "lint": "echo 'No linting setup for vanilla JS'"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}